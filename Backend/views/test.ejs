<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body>
    <form action="/admin" method="POST" enctype="multipart/form-data">
        <input type="file" name="image"><br>
        <label for="imgname">Image Name</label>
        <input type="text" name="imgname" id="imgname"><br>
        <label for="smdesc">Small Description</label>
        <input type="text" name="smdesc" id="smdesc"><br>
        <label for="desc">Detailed Description</label>
        <input type="text" name="desc" id="desc"><br>
        <label for="tags">Tags</label>
        <input type="text" id="tags"><br>
        <button class="add" onclick="add(event)">ADD</button><br>
        <label for="disp">display</label>
        <input type="text" name="tag" id="disp"><br>
        <button type="submit">Upload</button>
    </form>
    <script>
        var arr=[];
        function add(event){
            event.preventDefault();
            // arr.push($('#tags').val());
            var x = $('#disp').val();
            x = x + $('#tags').val() + ",";
            $('#tags').val("");
            $('#disp').val(x);
        }
        console.log(arr);
    </script>
</body>
</html>